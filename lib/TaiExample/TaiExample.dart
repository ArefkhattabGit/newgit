import 'package:flutter/material.dart';

class TaiExample extends StatelessWidget {
  const TaiExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
