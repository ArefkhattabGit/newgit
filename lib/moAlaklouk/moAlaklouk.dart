import 'package:flutter/material.dart';

class MoAlakloukEx extends StatelessWidget {
  const MoAlakloukEx({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
